import { LineSplitPipe } from './line-split.pipe';

describe('LineSplitPipe', () => {
  it('create an instance', () => {
    const pipe = new LineSplitPipe();
    expect(pipe).toBeTruthy();
  });
});
